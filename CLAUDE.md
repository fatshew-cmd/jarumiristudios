# Jarumiri Studios

## Dev workflow

- After finishing any task, verify `npm run dev` and `npm run tw` are still running. Big edits sometimes crash one or both of these — restart whichever has stopped before considering the task done.
